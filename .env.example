# Copy to .env.local for local development, and add the real values as
# environment variables in the Vercel project settings for production.

# Analytics loopt via Google Tag Manager (container GTM-NM7TZL29, hardcoded in
# index.html). GA4 wordt binnen GTM geconfigureerd, dus er is hier geen
# measurement ID meer nodig — VITE_GA_MEASUREMENT_ID is vervallen.

# Resend API key used by api/contact.js to send contact form submissions.
# Create one at https://resend.com after verifying a sending domain.
RESEND_API_KEY=

# Basis-URL van de Cloudflare R2-bucket met de video's (custom domain),
# zonder slash aan het eind, bv. https://media.tvm-productions.nl
# Leeg laten = video's uit public/videos/ (lokaal ontwikkelen).
VITE_VIDEO_BASE_URL=
